. {
    # Forward all non-internal queries to upstream DNS
    forward . 1.1.1.1 8.8.8.8
    cache 30
    errors
    log
    reload
}

example.corp {
    # Authoritative zone for internal domain — replaced by ORION setup wizard
    file /etc/coredns/zones/example.corp.db
    errors
    log
}
